"""Legacy tools from v1.0.x architecture.

DEPRECATED: These tools are kept for backward compatibility and internal use.
They are no longer exposed as MCP tools in v2.0.0.

- changes.py: Old map_changes implementation (replaced by detect_changes)
- config.py: initialize_config (now called internally by docmgr_init)
- memory.py: initialize_memory (now called internally by docmgr_init)
- workflows.py: Original monolithic workflows file (split into workflows/ subdirectory)
"""
